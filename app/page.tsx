const v0 = "v0"
const no = "no"
const op = "op"
const code = "code"
const block = "block"
const prefix = "prefix"

const Page = () => {
  return (
    <div>
      {/* rest of code here */}
      <p>{v0}</p>
      <p>{no}</p>
      <p>{op}</p>
      <p>{code}</p>
      <p>{block}</p>
      <p>{prefix}</p>
    </div>
  )
}

export default Page
